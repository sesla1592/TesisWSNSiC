 function printInfo(name, lastName) {
    alert("Name: " + name + " " + lastName);
//	 var arrayFromJava = document.myApplet.getPrueba();
//	 alert(arrayFromJava);
  }
 
 function showProgress(data, extraParam) { 
	 // Use "data" argument the usual way.
	 // The "extraParam" argument will contain "extraValue" in above example.
	 alert("Name: " + data + " " + extraParam);
	 } 
 